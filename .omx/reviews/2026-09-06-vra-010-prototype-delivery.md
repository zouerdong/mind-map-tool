# VRA-010：参考工作态与动效原型（交付记录）

日期：2026-09-06。执行依据：[VRA-010 卡](../plans/visual-alignment-task-cards-2026-09-06.md)。状态：**G-VIS 达成（2026-09-06 用户在原型演示中逐项确认，见文末）**。

## G-VIS 校准结果（2026-09-06，用户确认）

| 决策 | 结果 |
| --- | --- |
| D1（C-1/C-2）默认视觉 | **采纳**：暖白 #F9F8F4 + 近黑实心卡 + 橙强调；黑板同构提案值一并采纳；手绘圈保留可选。旧 PRD"纯白/纯黑/描边默认"条款按此覆盖（PRD 增补流程） |
| D3（C-4）眉题 | **采纳**：可选单行眉题，默认空 |
| D4（C-5）箭头/线型 | **采纳**：默认箭头 + 实/虚线外观 |
| D2（C-6）/D5/D6 | **全部采纳**：人工强调角色、不加分类系统、自由画布 + DAG 整理（不强制单父树） |
| 动效契约 | **采纳 800ms**（用户观看现场演示后确认） |
| **D7 整理方向（新决策）** | **默认横向 + 可选纵向**（随文档记忆）。用户指出 17 节点纵向单列不符合 MOV 形态（主线左、叶右列），经横向演示确认。推翻 brief"默认纵向"建议与 AC-15"垂直树"措辞——PRD 措辞更新由 VRA-020 承接（对账 C-3 升级为已裁决） |
| **D8 线形态语言（新决策）** | **散乱态平滑曲线 ↔ 整理后全正交圆角折线（"电路线"）**：除拐角圆弧（r≈12px）外只允许横/纵线段，禁止斜线；动画中连续过渡（形态参数随主运动插值）。经用户三轮迭代校法定稿（21 号截图起全正交）：**①跨层边内部直连优先**（源高度无遮挡不绕圈）；**②被挡才走贴边通道**（图边界外 56px）；**③目标侧梳状转折**（每边独占水平走廊 + 全缝唯一转折 x）→ **零共线重叠**（用户硬性要求），拓扑交叉允许。程序化检测 12/17 节点零重叠、零穿卡（24/25 号截图） |

G-VIS 达成后：VRA-020（schema/文件兼容）与 VRA-030（布局，含横向默认契约）可按指南并行派发；G-SCHEMA 派发前须指名 MRT-005×VRA-020 整合 owner（VRA-000 同伴审查观察）。

## 交付物

1. **合成夹具**（`tests/fixtures/visual/`，README 定规则）：
   - `reference-dag-12.json`：参考坐标版，复刻参考 12 节点/13 边五层构图（含 6 个多入边节点、孤立节点顶右、长文本卡自适应 84px 高示例），合成中英内容，经 `validateDocument` 校验通过；
   - `reference-dag-12-scattered.json`：同拓扑同内容散乱版（含负坐标），供 VRA-030/060 算法输入；
   - `reference-dag-12.visual.json`：眉题/强调/线型/外弧过渡标注 sidecar（schema 字段待 G-SCHEMA，生产不得读取）；
   - `motion-tree-17.json`：1 根 + 1 汇聚 + 15 叶，散乱起始坐标内置。
2. **规格文档**（`docs/product/`）：
   - `visual-state-tokens-2026-09-06.md`：暖白/黑板双主题 token（对比度实算）、排版几何、连线箭头、状态矩阵视觉规格（15 态）、极简 App Shell 提案、**D1–D6 决策包**（含旧文档视觉保留与文件兼容方向 A）；
   - `visual-motion-contract-2026-09-06.md`：800ms 时间线（错峰 ≤120ms、ease-out-cubic、叶子先动）、每帧一致性硬约束、打断/重入状态机、reduced-motion、规模降级、原型验收取样。
3. **隔离原型**（`tests/visual-prototype/reference-prototype.html`，自包含无依赖）：三场景（状态矩阵 / 12 节点动效 / 17 节点动效）× 暖白/黑板 × 1280×800/800×600 × reduced-motion × 关键帧步进（0/25/50/75/100%）× 中途打断 × undo 接续演示。
4. **证据**（`artifacts/visual-prototype-2026-09-06/`，README 索引）：17 张截图（16 态矩阵 + 动效关键帧）+ 2 段录像（12 节点完整整理；17 节点打断→接续→undo）。

## 验证（实际命令与结果）

- `validateDocument` × 3 夹具：全部 OK（12n/13e、12n/13e、17n/16e）；
- `git diff --check`：PASS；
- `pnpm test:unit`：29 files / 280 tests 全过（不破坏既有基线）；
- 原型 DOM 程序化检查：12 节点/13 边/5 强调/1 点线/1 虚线渲染正确；
- 视觉桥接核验：工作态方向与参考一致（暖白/橙首行/深卡/两层文字/底出顶入箭头/双外弧/孤立顶右/五层）；发现并修复长文本溢出（改多行 + 卡高自适应 84px，夹具同步）；
- 录像核验：连续移动、连线跟随无跳形、错峰减速、终态收敛参考构图。

## 关键决定

- 眉题/强调/线型/外弧不进 schema，用 sidecar 标注（G-SCHEMA 前）；
- 长文本卡以多行 + 增高处理（84px），演示"内容自适应不塞固定框"（brief §2）；
- 孤立节点在参考坐标版位于顶排最右（复刻参考）；多孤立成列策略留给 VRA-030；
- 动效打断/接续以 `SHOWN_POS`（当前显示坐标）为锚——生产等价物为 motion coordinator 的 view state（VRA-060）；
- 参考与同内容原型并排对照：`input/样式参考.JPG` ↔ `artifacts/visual-prototype-2026-09-06/01-work-light-1280.png` ↔ `12-motion12-settled.png`（同内容同坐标终态）。

## 未运行项 / 原因

- 生产 RF / Tauri 验证：不在本卡范围（隔离原型卡；生产接入 VRA-050/060/070）；
- 800×600 动效录像：静态档已证（16 号截图）；动画录像以 1280×800 主档录制；
- 真实 IME/保存/关闭交互：状态矩阵为视觉规格呈现，功能归 MRT-005/008 与 VRA-070。

## 风险 / 待决

- C-1～C-6 条款（对账报告 §3）待 G-VIS 一并裁决，其中 C-1/C-2（纯白纯黑 vs 暖白实心）直接决定本 token 表的正式化；
- 黑板配色为提案值，G-VIS 看板后定；
- 强调卡眉题对比 2.9:1 低于 4.5:1（按图形辅助处理，G-VIS 复核或加深）。

## 下一卡输入

- G-VIS 确认后 → VRA-020（样式模型 + 文件兼容 ADR；本卡 D1–D6 + sidecar 字段清单为其输入）与 VRA-030（布局契约 = 参考坐标版的层节奏 175px/同层净距/孤立列策略）可按指南并行；
- VRA-040 消费 token 表与连线/箭头/外弧规格；VRA-060 消费 motion contract 常量与状态机。
