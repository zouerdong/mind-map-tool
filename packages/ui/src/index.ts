// @mindmap/ui — 视觉与交互层（获选画布：React Flow，G1 批准）。
// 禁止依赖：操作系统 API / Tauri（check-boundaries 强制）。
// MM-050 将在此实现：SelectedCanvasProjection、InteractionController、编辑交互。
// 当前为 Bootstrap 占位。

export const UI_PACKAGE_VERSION = "0.1.0-bootstrap";
