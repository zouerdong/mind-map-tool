// @mindmap/platform — 平台适配层。
// 禁止依赖：React / React Flow / DOM（check-boundaries 强制）。
// MM-060 将在此实现：文件 port、commit protocol、TargetAuthorization/
// DocumentTargetHandle、LaunchRouter、偏好。
// Windows 适配器在本包内预留 port 结构（PRD §1.1 移植就绪约束）。

export const PLATFORM_PACKAGE_VERSION = "0.1.0-bootstrap";
