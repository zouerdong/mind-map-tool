{
  "name": "@mindmap/platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "平台能力 port 与 macOS 适配（文件、对话框、生命周期、偏好；MM-060 实现）",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@tauri-apps/api": "^2.2.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --root ../.. packages/platform"
  }
}
