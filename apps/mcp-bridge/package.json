{
  "name": "@mindmap/mcp-bridge",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "description": "MCP stdio 桥：Agent 参数化调用无头脑图导出（ADR 0014 §2）",
  "main": "src/server.ts",
  "types": "src/server.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "start": "tsx src/server.ts"
  },
  "dependencies": {
    "@mindmap/core": "workspace:*",
    "@mindmap/export": "workspace:*",
    "@mindmap/headless": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.18.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "vitest": "^2.1.8",
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0"
  }
}
