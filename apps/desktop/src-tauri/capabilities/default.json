{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Bootstrap 最小权限：仅核心默认能力。文件/对话框权限在 MM-060 按需加入。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
